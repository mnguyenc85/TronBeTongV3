<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="FlatGroupBoxStyle01" TargetType="GroupBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Background="{TemplateBinding Background}" Grid.ColumnSpan="3" Grid.Row="1" Grid.RowSpan="2" CornerRadius="3"/>
                        <Border Grid.Row="1" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1 1 0 0" CornerRadius="3 0 0 0"/>
                        <Border Grid.Row="1" Grid.Column="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0 1 1 0" CornerRadius="0 3 0 0"/>
                        <Border Grid.Row="2" Grid.ColumnSpan="3"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1 0 1 1"
                                CornerRadius="0 0 3 3">
                            <ContentPresenter/>
                        </Border>

                        <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>